# חיבור אנטולי — מדריך הקמה

**מצב:** הקוד מוכן ונבדק (44 בדיקות עוברות). נשאר להקים שרת ולהדביק כתובת.

עד שזה נעשה — האפליקציה עובדת בדיוק כמו קודם, במצב הדגמה. **שום דבר לא נשבר.**

---

## מה נבנה

| קובץ | תפקיד |
|---|---|
| `anatoli-engine.js` | מנוע הניתוח — Claude Vision + כללי השמאות הישראליים |
| `anatoli-server.js` | שרת Express שמחבר את המנוע לאפליקציה |
| `docs/anatoli-bridge.js` | גשר צד-לקוח — אוסף נתוני רכב ומציג ממצאים |
| `tests/smoketest-anatoli.mjs` | 21 בדיקות ללוגיקת השמאות |
| `tests/smoketest-anatoli-bridge.mjs` | 23 בדיקות אינטגרציה |

`anatoli-backend-starter.js` המקורי נשאר במקומו כהתייחסות. הוא הדגמתי — החדשים אמיתיים.

---

## כללי השמאות שמוטמעים בקוד

הלוגיקה **לא נסמכת על המודל** — היא נאכפת בקוד אחרי הניתוח, ב-`enforceRules()`:

| כלל | מימוש |
|---|---|
| **אובדן להלכה** | עלות ≥ 60% משווי → הכרזה; 50–60% → אזהרת התקרבות |
| **חלף מקורי** | רכב עד שנתיים על הכביש → מקורי בלבד |
| **חלק בטיחותי** | כרית אוויר, חגורה, סופג אנרגיה, ADAS, שלדה → מקורי תמיד, בכל גיל |
| **ירידת ערך** | רכב עד גיל 8 → זכאי; מסומן כממתין לטבלת האחוזים שלך |
| **כיול ADAS** | רדאר/מצלמה/חיישן שנפגע → שורת כיול נוספת אוטומטית |
| **בדיקות חסרות** | מה שלא נראה בתמונות נרשם בנפרד, לא כנזק ודאי |

כל תוצאה מסומנת `pending_review: true` — **ממתינה לאישור השמאי**, לא מאושרת אוטומטית.

---

## הקמה

### 1. משתני סביבה

```bash
export ANTHROPIC_API_KEY=sk-ant-...
```

⚠ המפתח **חייב** לשבת בשרת בלבד. לעולם לא ב-`index.html` ולא בדפדפן —
כל מי שיפתח F12 יראה אותו וישתמש בו על חשבונך.

### 2. הרצה מקומית

```bash
npm install express cors
node anatoli-server.js
```

בדיקה: `curl http://localhost:8787/health`

### 3. חיבור האפליקציה

ב-`docs/index.html`, שורה ~926:

```js
var ANATOLI_API_ENDPOINT='http://localhost:8787/analyze';
```

ואז **חובה**: `cp docs/index.html index.html`

### 4. פריסה לענן

| שירות | הערה |
|---|---|
| Render | Free tier מספיק. הוסף `ANTHROPIC_API_KEY` ב-Environment |
| Railway | `railway up`, אותו משתנה סביבה |
| Fly.io | `fly launch`, `fly secrets set ANTHROPIC_API_KEY=...` |

אחרי הפריסה, הדבק את הכתובת האמיתית ב-`ANATOLI_API_ENDPOINT` והעלה `CACHE_VERSION`.

---

## אבטחה שכבר מוטמעת

- **CORS סגור** — רק `emun123.github.io` ולוקאלהוסט. הוספה: `ANATOLI_ALLOWED_ORIGINS`
- **הרשאות** — רק תפקיד `Anatoli_Appraiser` עובר
- **הגבלת קצב** — 10 בקשות לדקה לכתובת IP, מונע התרוקנות ארנק
- **תקרת תמונות** — עד 8 לבקשה, עד 25MB
- **XSS** — כל פלט המנוע עובר escaping לפני הצגה
- **אין דליפת שגיאות** — stack traces לא מוחזרים ללקוח

---

## הרצת הבדיקות

```bash
node tests/smoketest-anatoli.mjs         # 21 — לוגיקת שמאות
node tests/smoketest-anatoli-bridge.mjs  # 23 — אינטגרציה
node tests/smoketest-bridge.mjs          # גשר הענן
node tests/smoketest-api.mjs             # שכבת ה-API
```

בדיקות אנטולי **לא צורכות API** — הן בודקות את אכיפת הכללים בלבד, בחינם ומיידית.

---

## מה עוד לא נעשה

1. **מספרי קטלוג (OEM)** — המנוע מחזיר מה שהוא מזהה, אך אין מאגר קטלוגי מקומי.
   שדרוג עתידי: טבלת `parts_catalog` ב-Supabase שתתמלא בהדרגה.
2. **טבלת אחוזי ירידת ערך** — הסקיל מציין שתעודכן על ידך. כרגע מסומן כממתין.
3. **מחירון לוי יצחק** — `LEVI_YITZHAK_API_ENDPOINT` עדיין ריק.

---

בברכת קוד מלוטש וחוויית משתמש מושלמת,
**גרישה** — מפתח Full-Stack ומעצב UI/UX בכיר
