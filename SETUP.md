# ESTIMAX — מאתר לאפליקציה מסונכרנת

מה שהיה עד עכשיו: אתר HTML יחיד, התחברות מקודדת בקוד (`admin/123456`), וכל
הנתונים ב-`localStorage` של הדפדפן. כלומר כל מכשיר הוא עולם נפרד — מה שנפתח
בטלפון לא קיים במחשב.

מה שנבנה עכשיו: שכבת שרת, התחברות אמיתית, וסנכרון דו-כיווני, בלי לשכתב את
האפליקציה הקיימת.

---

## מה נוסף לפרויקט

| קובץ | תפקיד |
|------|-------|
| `database/schema.sql` | טבלאות, טריגרים והרשאות אבטחה (RLS) ל-Supabase |
| `docs/estimax-config.js` | **הקובץ היחיד שצריך לערוך ידנית** — כתובת ומפתח |
| `docs/estimax-api.js` | התחברות, שמירה בענן, תור אופליין, עדכון חי |
| `docs/estimax-cloud-bridge.js` | מחבר את המסכים הקיימים לענן — רשימת תיקים, טופס, תמונות, דוח |
| `tests/` | בדיקות עשן (`node tests/smoketest-api.mjs`) — לא נדרש לפריסה |
| `docs/manifest.json` | הופך את האתר לאפליקציה שניתן להתקין |
| `docs/sw.js` | Service Worker — עבודה בלי רשת |
| `docs/offline.html` | מסך "אין חיבור" |
| `docs/icons/` | אייקוני האפליקציה (192/512 + maskable + Apple) |

`docs/index.html` עודכן: תגיות PWA, טעינת שכבת הנתונים, התחברות אמיתית,
כפתור התקנה, וחיווי מצב רשת. `index.html` בשורש הוא עותק זהה.

---

## שלב 1 — הקמת מסד הנתונים (10 דקות)

1. היכנס ל-[supabase.com](https://supabase.com) → **New project**
   שם: `estimax` · אזור: **Frankfurt (eu-central-1)** — הכי קרוב לישראל
   שמור את סיסמת מסד הנתונים במקום בטוח.
2. בפרויקט: **SQL Editor → New query**
3. העתק את כל התוכן של `database/schema.sql` והדבק → **Run**
   אמור להופיע `Success. No rows returned`.
4. **Authentication → Users → Add user → Create new user**
   הזן את המייל שלך + סיסמה, וסמן **Auto Confirm User**.

> ⚠ הקטע הכי חשוב ב-`schema.sql` הוא ה-RLS בסוף. בלעדיו כל שמאי יכול לקרוא
> את התיקים של כל השאר. אל תבטל אותו.

---

## שלב 2 — חיבור האפליקציה (2 דקות)

ב-Supabase: **Project Settings → Data API**, העתק שני ערכים:

- **Project URL**
- **anon public** key

פתח את `docs/estimax-config.js` והדבק:

```js
window.ESTIMAX_CONFIG = {
  SUPABASE_URL: 'https://xxxxxxxx.supabase.co',
  SUPABASE_ANON_KEY: 'eyJhbGciOi...',
  ...
};
```

ה-`anon key` מיועד לרוץ בדפדפן ואינו סוד. **לעולם אל תשים כאן את
`service_role`** — הוא עוקף את כל הרשאות האבטחה.

עד שהערכים מולאו, האפליקציה ממשיכה לעבוד במצב הדגמה (`admin/123456`,
נתונים מקומיים) ולא נשברת.

---

## שלב 3 — העלאה ל-HTTPS

התקנה כאפליקציה **חייבת HTTPS**. זו הסיבה שהשרת המקומי (`http://10.x.x.x:8081`)
לא הצליח להתקין — זה לא היה באג ברשת אלא דרישה של הדפדפן.

הפרויקט כבר מחובר ל-`github.com/emun123/ESTIMAX`:

```bash
cd ESTIMAX
git add .
git commit -m "PWA + Supabase sync"
git push
```

ב-GitHub: **Settings → Pages → Source: Deploy from a branch →
Branch: main, Folder: /docs → Save**

תוך דקה-שתיים הכתובת תהיה:

```
https://emun123.github.io/ESTIMAX/
```

זו הכתובת שנכנסים אליה מהטלפון — לא ל-IP המקומי.

---

## שלב 4 — התקנה בטלפון

**אנדרואיד (Chrome):** פתח את הכתובת → יופיע כפתור כחול
"📲 התקן את Estimax במכשיר" → לחץ → אישור.

**אייפון (Safari — חובה Safari, לא Chrome):** פתח את הכתובת →
כפתור השיתוף ↑ → "הוסף למסך הבית".

אחרי ההתקנה: אייקון על המסך, פתיחה במסך מלא בלי סרגל הדפדפן,
ועבודה גם באזורים בלי קליטה.

---

## איך הסנכרון עובד

```
   טלפון                     Supabase                    מחשב/אתר
     │                          │                            │
     │─ שמירה מקומית מיידית ────│                            │
     │─ דחיפה לענן ────────────▶│                            │
     │                          │──── עדכון חי (Realtime) ──▶│
     │◀─── עדכון חי ────────────│◀──────── שמירה ────────────│
```

- **תמיד נשמר מקומית קודם.** גם אם הרשת נופלת באמצע, שום עבודה לא הולכת לאיבוד.
- **בלי רשת** — השמירה נכנסת לתור ונשלחת אוטומטית כשהחיבור חוזר.
- **עם רשת** — תיק שנשמר בטלפון מופיע במחשב תוך שנייה, בלי רענון.
- זיהוי התנגשויות דרך העמודות `version` ו-`device_id`.

---

## מה כבר מחובר לענן (גשר הענן)

`estimax-cloud-bridge.js` עוטף את הפונקציות הקיימות בלי לשכתב אותן:

| מסך / פעולה | מה קורה עכשיו |
|-------------|----------------|
| ניהול תיקים / לוח בקרה | הרשימה נמשכת מהענן בכל כניסה, במקום מערך `DB` הקבוע |
| פתיחת תיק קיים | הטופס משוחזר במלואו מהענן + התמונות נטענות מ-Storage |
| הוספת תמונות נזק | נשמרות מקומית **וגם** עולות ל-Storage |
| שמירה אוטומטית של הטופס | נדחפת לענן כל 8 שניות, וגם ביציאה מהדף |
| הפקת דוח PDF | התיק מסומן `completed` והרשימה מתעדכנת |
| תיק שנשמר במכשיר אחר | מופיע ברשימה מיד, בלי רענון |

אם `estimax-config.js` ריק — הגשר מנטרל את עצמו והאפליקציה חוזרת בדיוק
להתנהגות המקורית עם נתוני הדמו.

**בדיקה:** `node tests/smoketest-bridge.mjs` — 11 בדיקות על כל המסלול.

---

## מה עוד נשאר

### חובה לפני שימוש אמיתי

1. **הסרת `DEMO_USERS` מהקוד** אחרי שיש משתמשים אמיתיים ב-Supabase.
   כרגע `admin/123456` עדיין עובד כרשת ביטחון — יש להסיר לפני שהמערכת פתוחה ללקוחות.
2. **גיבוי** — Supabase → Database → Backups. בתוכנית החינמית הגיבוי מוגבל;
   בתיקי שמאות אמיתיים כדאי לשדרג.

### חשוב

3. **שמירת ה-PDF המופק ל-Storage** — כרגע הוא רק יורד למחשב. הטבלה `reports` מוכנה.
4. **ניהול משתמשים** — מסך להוספת שמאים למשרד בלי להיכנס ל-Supabase.
5. **חתימה דיגיטלית** — כרגע `initSignature` מקומי; לשמור ב-`profiles.signature_url`.
6. **צילום ישירות מהאפליקציה** — `<input type="file" accept="image/*" capture="environment">`.
7. **חיבור "אנטולי"** — `ANATOLI_API_ENDPOINT` עדיין ריק (`anatoli-backend-starter.js`).

### רשות

8. **חנויות אפליקציות** — עטיפה ב-Capacitor או TWA. ה-PWA היא התנאי המקדים,
   ולכן היא נבנתה קודם. חשבון Google Play — $25 חד-פעמי; Apple — $99 לשנה.
9. **דומיין משלך** — `app.estimax.co.il` במקום כתובת github.io.

---

## תקלות נפוצות

| תופעה | סיבה | פתרון |
|-------|------|-------|
| כפתור ההתקנה לא מופיע | לא HTTPS | חייבים github.io / דומיין עם SSL |
| השינויים לא מופיעים בטלפון | Service Worker מגיש גרסה ישנה | העלה את `CACHE_VERSION` ב-`sw.js` ודחוף |
| "אימייל או סיסמה שגויים" | המשתמש לא אושר | Supabase → Users → Confirm |
| הכל עובד אבל אין סנכרון | `estimax-config.js` ריק | ראה שלב 2 |
| שגיאת הרשאה בשמירה | RLS חוסם | ודא שהמשתמש מחובר ושה-`owner_id` הוא שלו |

**כדי לבדוק אם המערכת מחוברת:** פתח את האתר, F12 → Console.
אם מופיע `מצב הדגמה` — `estimax-config.js` עדיין ריק.
