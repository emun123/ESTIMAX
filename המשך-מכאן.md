# ESTIMAX — נקודת המשך

**עודכן:** 5 באוגוסט 2026
**מצב:** הקוד הושלם, נדחף ל-GitHub, והאתר החי עובד. נשאר רק להקים את Supabase.

---

## ✅ מה הושלם

**הקוד** — commit `045c24a` על ענף `main`, 19 קבצים.

**האתר החי** — https://emun123.github.io/ESTIMAX/
GitHub Pages מוגדר כעת על `main` + תיקיית `/docs`. כל push עתידי מתפרסם אוטומטית.
אומת בפועל: `viewport-fit=cover`, `theme-color`, תגיות iOS, `manifest.json`,
וכפתור "📲 התקן את Estimax במכשיר" מופיעים בדף.

**מה נבנה:**

| קובץ | תפקיד |
|------|-------|
| `database/schema.sql` | טבלאות + טריגרים + RLS ל-Supabase |
| `docs/estimax-config.js` | **הקובץ היחיד לעריכה ידנית** — 2 ערכים |
| `docs/estimax-api.js` | התחברות, שמירה בענן, תור אופליין, Realtime |
| `docs/estimax-cloud-bridge.js` | מחבר את המסכים הקיימים לענן (monkey-patch) |
| `docs/manifest.json` + `sw.js` + `icons/` + `offline.html` | שכבת ה-PWA |
| `tests/` | 2 בדיקות עשן — כולן עוברות |
| `העלה-לאינטרנט.bat` | commit + push בלחיצה כפולה |

---

## ⬜ מה נשאר — Supabase (20 דקות)

1. **supabase.com** → New project → שם `estimax` → אזור **Frankfurt (eu-central-1)**
2. **SQL Editor → New query** → הדבק את כל `database/schema.sql` → **Run**
3. **Authentication → Users → Add user** → המייל שלך + סיסמה + סמן **Auto Confirm User**
4. **Project Settings → Data API** → העתק `Project URL` ו-`anon public key`
5. הדבק אותם ב-`docs/estimax-config.js`
6. לחיצה כפולה על `העלה-לאינטרנט.bat`

עד שזה נעשה — האפליקציה עובדת במצב הדגמה (`admin` / `123456`, נתונים מקומיים).
היא לא שבורה, פשוט לא מסונכרנת.

**איך לבדוק שזה עבד:** פתח את האתר, F12 → Console.
אם מופיע "מצב הדגמה" — `estimax-config.js` עדיין ריק.

---

## הערות חשובות לפעם הבאה

- **מטמון:** GitHub Pages שומר גרסאות עד ~10 דקות. אם נראה שהעדכון לא עלה,
  הוסף `?v=2` לכתובת לפני שמחפשים באג. בזבזנו על זה זמן.
- **שני עותקים:** `index.html` בשורש ו-`docs/index.html` חייבים להישאר זהים.
  `docs/` הוא מה שמתפרסם. אחרי כל שינוי: `cp docs/index.html index.html`.
- **`sw.js`:** אחרי כל שינוי בקוד יש להעלות את `CACHE_VERSION`, אחרת מכשירים
  שכבר התקינו ימשיכו לראות את הגרסה הישנה.
- **שני clones:** יש עותק נוסף של הריפו במקום אחר במחשב (הוא שהיה ב-GitHub Desktop
  לפני שהוספתי את זה שב-`cowork`). לעבוד רק מ-`cowork\ESTIMAX` כדי לא לפצל היסטוריה.
- **`gh-pages`:** הענף הזה עדיין קיים בריפו ואינו בשימוש יותר. אפשר למחוק אותו.
- **תוסף Claude for Chrome:** לא היה מחובר בשיחה הזו. אם יחובר, אפשר להריץ את כל
  הקמת Supabase (סעיפים 2–5 למעלה) אוטומטית — למעט ההרשמה עצמה.

---

## נוסף בסבב האחרון (טרם נדחף ל-GitHub)

- **PDF → Storage** — הדוח המופק עולה לבאקט `reports` ונרשם בטבלה.
  מומש ביירוט `PDF_CLOUD_ENDPOINT` כדי לא לשכפל את לוגיקת יצירת ה-PDF.
- **חתימה דיגיטלית** — `saveSignature()` שומרת ל-`profiles.signature_url`
  (הקובץ תחת `<uid>/_signature/` בבאקט `case-images`, ללא שינוי בסכמה).
- **📷 צלם עכשיו** — כפתור שפותח את מצלמת הטלפון ישירות לגלריית הנזק.
- **מסך הגדרות** — "הגדרות" בסרגל הצד פותח עריכת פרופיל (שם, רישיון,
  טלפון, משרד) + תצוגת החתימה השמורה.
- `CACHE_VERSION` הועלה ל-`estimax-v2`.
- הבדיקות הורחבו ל-14 — כולן עוברות (`node tests/smoketest-bridge.mjs`).

**⚠ השינויים האלה קיימים רק מקומית. הרץ `העלה-לאינטרנט.bat` כדי לפרסם.**

---

## אחרי Supabase — לפי סדר עדיפות

1. **להסיר את `DEMO_USERS`** מ-`index.html` (שורה ~695). `admin/123456` עדיין עובד.
2. **גיבוי** — Supabase → Database → Backups.
3. **הזמנת שמאים מתוך האפליקציה** — דורש Edge Function (אסור להחזיק
   `service_role` בדפדפן). עד אז: Supabase → Authentication → Add user.
4. **חיבור "אנטולי"** — `ANATOLI_API_ENDPOINT` עדיין ריק.
5. **הצגת החתימה השמורה בדוח עצמו** (כרגע רק בהגדרות).
6. חנויות אפליקציות (Capacitor/TWA) — ה-PWA היא התנאי המקדים והיא מוכנה.

הפירוט המלא ב-`SETUP.md`.
