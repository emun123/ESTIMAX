# ESTIMAX — נקודת המשך

**עודכן:** 17 באוגוסט 2026
**מצב:** מנוע אנטולי נבנה, נבדק, ונדחף. נשארו שתי משימות שדורשות מפתחות שלך.

---

## ✅ הכל בענן

כל הקוד ב-`main`. **אין עבודה מקומית ממתינה** — אין צורך להריץ `העלה-לאינטרנט.bat`.

**אימות שבוצע:** clone נקי מהמאגר + הרצת 44 הבדיקות מול הקוד שבענן — כולן עברו.

### מה נוסף בסבב האחרון

| קובץ | תפקיד |
|---|---|
| `anatoli-engine.js` | Claude Vision + כללי שמאות ישראליים נאכפים בקוד |
| `anatoli-server.js` | שרת Express — CORS סגור, הגבלת קצב, בקרת הרשאות |
| `docs/anatoli-bridge.js` | אוסף נתוני רכב מהטופס, מציג ממצאים מלאים |
| `ANATOLI-SETUP.md` | מדריך הקמת השרת |
| `tests/smoketest-anatoli*.mjs` | 44 בדיקות |

**הבאג המהותי שתוקן:** `sendToAnatoli` שלחה תמונות בלבד. עכשיו גם שנה, שווי,
ק"מ ותאריך עלייה לכביש — בלעדיהם אי אפשר לחשב אובדן להלכה, כלל מקורי/חליפי,
או ירידת ערך.

**באג נוסף ב-`sw.js`:** הבדיקה מול `NEVER_CACHE` השתמשה ב-`url.hostname` בלבד,
כך שנתיב `/analyze` מעולם לא נתפס. תוקן ל-`hostname + pathname`.

**כללים נאכפים בקוד** (לא בפרומפט — מודל יכול לטעות, חוק לא):
אובדן להלכה 60%, מקורי עד שנתיים, חלק בטיחותי תמיד מקורי,
ירידת ערך עד גיל 8, כיול ADAS אוטומטי.

`CACHE_VERSION` = `estimax-v3`. שני עותקי `index.html` זהים (197,922 בייט).

---

## ⬜ שתי משימות — שתיהן דורשות מפתחות שלך

### 1. Supabase (20 דקות) — החסם העיקרי

1. **supabase.com** → New project → `estimax` → **Frankfurt (eu-central-1)**
2. **SQL Editor → New query** → הדבק `database/schema.sql` → **Run**
3. **Authentication → Users → Add user** → מייל + סיסמה + **Auto Confirm User**
4. **Project Settings → Data API** → העתק `Project URL` ו-`anon public key`
5. הדבק ב-`docs/estimax-config.js`, ואז `cp docs/estimax-config.js .`
6. העלה `CACHE_VERSION` ל-`estimax-v4` ודחוף

עד אז: מצב הדגמה (`admin`/`123456`, נתונים מקומיים). לא שבור, רק לא מסונכרן.

### 2. שרת אנטולי — פרטים ב-`ANATOLI-SETUP.md`

צריך `ANTHROPIC_API_KEY` **בשרת בלבד** ופריסה ל-Render/Railway.
אחר כך הדבק את הכתובת ב-`ANATOLI_API_ENDPOINT` (שורה 926 ב-`docs/index.html`).

---

## ⚠ מלכודות — קרא לפני שנוגעים בקוד

- **עורך גיטהאב מכפיל קבצים.** CodeMirror מרנדר רק שורות גלויות, ולכן
  select-all תופס רק אותן וההדבקה **נוספת במקום להחליף**. קרה פעמיים:
  ב-`index.html` (197KB) וב-`המשך-מכאן.md`. **תמיד אמת אחרי עריכה בממשק**
  (ספור `<!DOCTYPE` או השווה גודל). הדרך הבטוחה: מחק את הקובץ וצור מחדש
  דרך `/new/main` — שם העורך מתחיל ריק. או פשוט עבוד מקומית עם Git.
- **מטמון:** GitHub Pages שומר עד ~10 דקות. הוסף `?v=3` לכתובת לפני שמחפשים באג.
- **שני עותקים:** `index.html` בשורש ו-`docs/index.html` חייבים להיות זהים.
  אחרי כל שינוי: `cp docs/index.html index.html`
- **`sw.js`:** אחרי כל שינוי — העלה `CACHE_VERSION`. קובץ חדש? הוסף ל-`PRECACHE`.
- **`DEMO_USERS`** (שורה ~695): **אל תסיר לפני Supabase.** זו דרך הכניסה
  היחידה כרגע — הסרה תנעל את האפליקציה לגמרי.
- **מפתחות:** `ANTHROPIC_API_KEY` בשרת בלבד. `anon key` של Supabase כן בדפדפן
  (מוגן ב-RLS). `service_role` — לעולם לא.
- **שני clones:** לעבוד רק מ-`cowork\ESTIMAX`.
- **`gh-pages`:** ענף מת, אפשר למחוק.

---

## עבודה שאפשר לעשות בלי מפתחות

1. **הצגת החתימה בדוח עצמו** — כרגע נשמרת בהגדרות אך לא מופיעה בדוח המודפס.
   פער גלוי בתוצר שהלקוח מקבל. עצמאי לגמרי.
2. **טבלת אחוזי ירידת ערך** — המנוע מסמן "ממתין לטבלה". אפשר לבנות מבנה
   וחישוב, ואתה רק תמלא אחוזים.
3. **תשתית `parts_catalog`** — טבלה שתתמלא ממספרי קטלוג שאנטולי מזהה,
   כדי לא לשלם על זיהוי חוזר של אותו חלק.
4. **ליטוש UI/UX** — שלב 7 של גרישה, טרם בוצע.

## אחרי Supabase

5. **הסרת `DEMO_USERS`** — רק אחרי אימות התחברות אמיתית.
6. **גיבוי** — Supabase → Database → Backups.
7. **מחירון לוי יצחק** — `LEVI_YITZHAK_API_ENDPOINT` ריק (שורה 933).
8. **הזמנת שמאים מהאפליקציה** — דורש Edge Function.
9. **חנויות אפליקציות** (Capacitor/TWA) — ה-PWA מוכנה.

הפירוט המלא ב-`SETUP.md` ו-`ANATOLI-SETUP.md`.
